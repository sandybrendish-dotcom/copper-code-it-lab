# Linux Administration Notes

## Core commands

```bash
ip addr
ip route
ss -tulpn
systemctl status SERVICE
journalctl -u SERVICE
df -h
lsblk
free -h
top
```

Add explanations and examples as each command is used in a real project.
